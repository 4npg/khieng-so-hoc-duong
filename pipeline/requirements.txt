# Core HTTP & image handling
requests>=2.31.0
pillow>=10.2.0

# Environment variable loading (.env.local / .env support)
python-dotenv>=1.0.1

# Data validation (used for potential API response schemas)
pydantic>=2.6.0

# CLI interface for pipeline scripts
click>=8.1.7

# Typing backport for Python < 3.10 (X | Y union syntax in type hints)
typing-extensions>=4.9.0
